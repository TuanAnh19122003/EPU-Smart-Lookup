
// Lighter, friendlier palette suitable for learning / academic environment
const colors = {
    // soft blue -> mint gradient background
    gradientStart: '#E8F5FF',
    gradientEnd: '#E8FFF2',
    // surfaces
    card: '#FFFFFF',
    // primary action color (readable on white)
    primary: '#1976D2',
    // warm accent for highlights
    accent: '#FFB74D',
    // main text (dark for good contrast)
    text: '#102A43',
    // muted text / secondary
    muted: '#4A6172',
}

const spacing = {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 40,
}

const typography = {
    brandShort: 'EPU',
}

export { colors, spacing, typography }
